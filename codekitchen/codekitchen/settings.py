from private_settings import (
    DEBUG,
    TEMPLATE_DEBUG,
    ALLOWED_HOSTS,
    AUTH_ROOT_URL,
    DATABASES,
    STATIC_URL,
    EMAIL_BACKEND,
    EMAIL_HOST,
    EMAIL_PORT,
    EMAIL_HOST_USER,
    EMAIL_HOST_PASSWORD,
    EMAIL_PREFIX,
        )

from shared_private import (
    SECRET_KEY,
        )

from shared_settings import (
    BASE_DIR,
    REPO_DIR,
    INSTALLED_APPS,
    MIDDLEWARE_CLASSES,
    LOGIN_REDIRECT_URL,
    ROOT_URLCONF,
    WSGI_APPLICATION,
    LANGUAGE_CODE,
    TIME_ZONE,
    USE_I18N,
    USE_L10N,
    USE_TZ,
    STATICFILES_DIRS,
    TEMPLATE_DIRS,
    STATIC_ROOT,
        )
