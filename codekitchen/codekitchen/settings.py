from private_settings import (
    DEBUG,
    TEMPLATE_DEBUG,
    ALLOWED_HOSTS,
    DATABASES,
    STATIC_URL,
        )

from shared_private import (
    SECRET_KEY,
        )

from shared_settings import (
    BASE_DIR,
    REPO_DIR,
    INSTALLED_APPS,
    MIDDLEWARE_CLASSES,
    ROOT_URLCONF,
    WSGI_APPLICATION,
    LANGUAGE_CODE,
    TIME_ZONE,
    USE_I18N,
    USE_L10N,
    USE_TZ,
    STATICFILES_DIRS,
    TEMPLATE_DIRS,
    STATIC_ROOT,
        )
