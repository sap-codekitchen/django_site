from private_settings import (
    DEBUG,
    TEMPLATE_DEBUG,
    ALLOWED_HOSTS,
        )

from shared_private import (
    SECRET_KEY,
    DATABASES,
        )

from shared_settings import (
    BASE_DIR,
    REPO_DIR,
    INSTALLED_APPS,
    MIDDLEWARE_CLASSES,
    ROOT_URLCONF,
    WSGI_APPLICATION,
    LANGUAGE_CODE,
    TIME_ZONE,
    USE_I18N,
    USE_L10N,
    USE_TZ,
    STATIC_URL,
    STATICFILES_DIRS,
    TEMPLATE_DIRS,
    STATIC_ROOT,
        )
